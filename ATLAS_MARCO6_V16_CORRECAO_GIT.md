# Projeto Atlas — Marco 6 v16 corrigido

Esta entrega consolida Render + Supabase e adiciona proteção Git.

- `render.yaml` para Render Blueprint;
- PostgreSQL Supabase;
- Storage privado `atlas-animal-media`;
- `.gitignore` reforçado;
- `scripts/dev/restore_git_metadata.ps1`;
- `scripts/quality/audit_git_secrets.ps1`.

Arquivos reais de segredo, como `backend/.env`, não são incluídos.
