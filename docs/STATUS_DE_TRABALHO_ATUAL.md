# Status de trabalho atual — Atlas

Atualizado em 26/09/2026.

## Estado

- Proteção contínua de Operações concluída localmente (100%): rota observa contexto e invalida permanentemente a Central após perda de conta/fazenda/autorização; leitura obsoleta é descartada e gravação confere contexto antes de ler e imediatamente antes de chamar persistência. Falha de salvar informa ausência de sucesso e recarrega dados anteriores quando o contexto ainda é válido. 31 testes aprovados, análise de quatro arquivos sem apontamentos, diff aprovado e build Windows profile oficial único aprovado em 131,2 s. Checkpoint identificado pela tag atlas-operations-live-context-20260926. Aplicativo aberto/celular preservados, nova compilação ainda não instalada/aberta. Limites: somente mudanças conhecidas do controlador; escrita já entregue ao plugin não é cancelada, armazenamento legado não foi migrado por tenant/empresa. Próximo marco: isolar armazenamento por contexto verificável, sem atribuir dados antigos por nome, e avaliar versão agrupada entre fazendas. backend/atlas_test.db preservado.

- Proteção contínua de Operações, 85%: guarda observa notificações de sessão e invalida permanentemente a rota ao perder usuário/empresa/tenant/fazenda autorizada. Central recebe verificador, descarta leitura obsoleta e fila de escrita verifica antes da leitura e imediatamente antes de solicitar persistência. Recusa não apresenta sucesso e falha de gravação informa/recarrega dados anteriores. 31 testes aprovados (quatro novos de interface/fila), análise/build em fechamento. Não cancela escrita que já foi entregue ao plugin de armazenamento; não é isolamento completo por tenant.

- Proteção contínua de Operações, 0%: invalidar tela após alteração de conta/fazenda/acesso, reconferir contexto na fila de persistência e tratar recusa sem sucesso aparente. Componentes: guarda de rota, Central, repositório e testes de revogação durante escrita/interface. Validações: testes focalizados, análise, diff e build único Windows agrupado. Próximo marco: contexto perdido não permite concluir gravação pendente; armazenamento legado por tenant continua etapa separada.

- Navegação de Operações concluída localmente (100%): removida abertura global pelo dashboard, Campo usa mesma rota autorizada e Central exige farmId. Sessão ausente/não autenticada, fazenda inativa/divergente, empresa/tenant diferentes e acesso não concedido recusam a entrada sem rede/escrita. 27 testes aprovados, análise de cinco arquivos sem apontamentos, diff e build Windows profile oficial aprovados (139,8 s), agrupando três correções de Operações. Checkpoint identificado pela tag atlas-operations-authorized-navigation-20260926. Não aberto/instalado sobre a janela em uso ou celular; ensaio operacional pendente. Limites: autorização é conferida na entrada, armazenamento legado ainda não isolado por tenant/empresa e revogação com tela aberta não foi implementada. Próximo marco: reforçar persistência/uso com contexto autorizado e realizar ensaio agrupado entre fazendas. backend/atlas_test.db preservado.

- Navegação de Operações, 95%: 27 testes aprovados (sete novos), análise de cinco arquivos sem apontamentos, diff aprovado e roteiro de ensaio atualizado. Dashboard e Campo compartilham validação na entrada da rota; Central exige ID de fazenda. Compilação Windows profile oficial única em fechamento, agrupando também remoção de exemplos e salvamento por fazenda; não substitui a janela aberta nem atualiza o celular. Próximo marco: hash/identidade e checkpoint publicado.

- Navegação de Operações, 80%: dashboard e Campo usam a mesma rota com validação de sessão autenticada, empresa, tenant, fazenda ativa e acesso explícito; atalho antigo com fazenda divergente é recusado. Central exige farmId no construtor, removendo a entrada global. Aviso não inicia rede nem modifica dados. Testes de autorização, administrador, contexto ausente, aviso na interface e contratos das duas entradas implementados; regressão/análise em fechamento. Proteção é na entrada da rota, não monitoramento de revogação enquanto tela aberta.

- Pacote navegação de Operações, 0%: remover acesso global pelo dashboard, exigir farmId na Central e validar sessão/fazenda da rota. Componentes previstos: navegação compartilhada, dashboard, entrada por Campo, Central e testes. Validações: contexto autorizado/ausente, isolamento, testes focalizados, análise estática e diff. Próximo marco: nenhuma entrada de produção na Central sem fazenda explícita; preservar registros legados e não migrar dados por nome. Aplicativo aberto/celular não serão interrompidos.

- Pacote Operações/Campo concluído localmente (100%): persistência da Central aberta por Campo agora substitui somente operações da fazenda exata, preservando outras fazendas/legado e campos desconhecidos. Consulta específica não inclui registros sem fazenda. Validação prévia, serialização no processo e falhas sem sobrescrita; 20 testes aprovados (sete novos), análise de três arquivos sem apontamentos e diff aprovado. Checkpoint identificado pela tag atlas-operations-scoped-save-20260926. Sem alteração visual/build; aplicativo aberto e celular ainda mantêm a versão anterior até atualização agrupada. Limites: abertura legada do dashboard sem farmId mantém comportamento global; isolamento completo por tenant/empresa e concorrência entre processos não foram entregues. Próximo marco: revisar entrada legada e contexto autorizado antes de expandir operações, depois ensaios Campo/offline. backend/atlas_test.db preservado.

- Operações/Campo, 85%: gravação filtrada faz merge por ID exato de fazenda e preserva dados de outras fazendas, registros sem vínculo e campos legados desconhecidos. Escritas serializadas entre instâncias no mesmo processo; IDs repetidos/alheios, escopo inválido e conteúdo corrompido impedem substituição. Central passa a fazenda ao salvar; consulta específica não atribui legado sem vínculo. Vinte testes aprovados, sete novos; análise/diff/checkpoint em fechamento. Serialização não cobre múltiplos processos e não substitui isolamento completo por tenant/empresa.

- Pacote Operações/Campo, 0%: corrigir substituição global ao salvar lista filtrada por fazenda, impedir atribuição implícita de tarefas sem proprietário e serializar gravações no processo. Componentes: repositório, chamada de persistência da Central e testes de duas fazendas/concorrência/recusa. Validações: testes focalizados, análise estática e diff; sem mudança visual ou build neste pacote. Próximo marco: salvar uma fazenda preservando integralmente outras e registros legados. Sem migração de dados ou promessa de isolamento por tenant neste repositório legado.

- Pacote de retomada concluído localmente (100%): criação automática de exemplos removida da Central de Operações; registros preexistentes preservados. Treze testes aprovados, análise de dois arquivos sem apontamentos e diff aprovado. Sem alteração visual/build/instalação neste pacote para economizar recursos; aplicativo aberto mantém versão anterior até atualização agrupada. Checkpoint identificado pela tag atlas-operations-no-auto-examples-20260926. Cronograma consolidado em nove pacotes de fechamento; demonstração adiada. Próxima etapa: ensaios Campo/offline e fechamento das salvaguardas operacionais restantes, avançando localmente onde houver dependência externa. Banco backend/atlas_test.db e celular preservados.

- Retomada operacional, 85%: Central de Operações não cria mais tarefas/custos de exemplo ao consultar armazenamento vazio; nenhuma exclusão ou migração dos registros existentes. Treze testes aprovados, incluindo três novos de ausência de escrita e preservação dos dados. Cronograma reorganizado em nove pacotes de fechamento comercial, distinguindo homologações externas, expansão e entrega. Próximo marco: análise/diff e checkpoint; atualização do aplicativo aberto será agrupada, não aplicada nesta execução.

- Retomada operacional, 0%: demonstração adiada por solicitação do usuário. Pacote local: retirar criação automática de exemplos na Central de Operações sem apagar registros existentes; consolidar sequência de homologação e escopo da primeira versão comercial. Arquivos previstos: repositório de operações, teste de persistência, cronograma e status. Validações: testes focalizados de operações/Campo, análise estática e diff. Sem alteração visual, instalação no celular ou sondagem externa. Próximo marco: operações reais preservadas e critérios de fechamento explícitos.

- Reversão solicitada pelo usuário (26/09, concluída): removidos entrada, scripts, testes e documentação exclusivos da demonstração; runner Windows e script de carga restaurados ao checkpoint anterior. Servidor local demonstrativo encerrado; executáveis de apresentação desativados em pasta renomeada recuperável. Atlas oficial Debug existente aberto na tela de entrada, sem automatizar autenticação. Nenhum dado fictício inserido na conta oficial; PIN, celular e backend/atlas_test.db preservados. Próximo passo depende da nova orientação do usuário, sem retomar a demonstração separada.

- Pacote concluído (26/09, 100% da entrega agrupada): Windows profile de avaliação 3 com URL oficial, código dd47b57, reúne cache de piquetes e painel Campo local-primeiro. Build único aprovado em 110,4 s; 25 testes/análise do código anterior reutilizados, sem repetição por ausência de mudanças. Usuário confirmou salvar/fechar; processo anterior ausente antes da abertura. Nova janela PID 6556 responsiva; captura conferiu login com Entrar offline com PIN disponível, sem autenticação automatizada. SHA-256 Profile/data/app.so B930FE5959F74B001BC5239CBA8AD35AC05122A35489D39EF6DEE7971A0C5BA8. Roteiro docs/pilot/AVALIACAO_CAMPO_OFFLINE.md e cronograma atualizados; checkpoint publicado pela tag atlas-field-grouped-preview3-20260926. Próximo marco: usuário consultar piquetes uma vez, conferir Campo/Suporte e reabrir sem internet com seu PIN. Esta abertura não comprova o ensaio offline completo. Celular e banco existente preservados; não é release de distribuição.

- Planejamento da avaliação 3 cumprido: registrado em 0%, atualizado a 85% após build/hash/fechamento e concluído em 100% após abertura e captura. Nenhuma mudança funcional adicional ao dd47b57 neste pacote.

- Pacote concluído localmente (26/09, 100%): painel Campo inicia somente pela leitura local autorizada, sem espera pela API de piquetes; Atualizar consulta o servidor com timeout de oito segundos. Cópia e data preservadas em falha de rede; rejeição explícita 401/403 não exibe cópia como resposta válida. Fontes ausentes são “não consultado/conferido”, sem falso zero; alertas críticos conhecidos prevalecem sobre dados parciais. Leitura de operações do painel não cria novas sementes nem inclui registros sem fazenda. Outros fluxos legados de operações não foram migrados. Vinte e cinco testes aprovados (nove novos), análise dos quatro arquivos com uma chave faltante corrigida e conferência focalizada sem problemas; build Windows debug oficial e diff aprovados. Checkpoint publicado identificado pela tag atlas-field-panel-offline-20260926. Próxima etapa: versão agrupada de avaliação e ensaio real de consulta/reabertura sem internet. Aplicativo aberto, celular e banco existente preservados; esta alteração ainda não está instalada neles.

- Planejamento cumprido do pacote: leitor isolado por contexto, repositório de operações, painel e testes; registrado inicialmente em 0%, atualizado em 75% após testes e concluído em 100% após compilação. Não equivale à conclusão global do projeto ou à homologação em aparelho.

- Pacote concluído localmente (26/09, 100%): consulta local-primeiro de piquetes por tenant/empresa/fazenda, data visível e Atualizar remoto explícito. Sem cache, consulta oficial inicial; cópia local não confirma CRUD. Alterações confirmadas invalidam a cópia e solicitam releitura; mudança de escopo descarta a exibição anterior. Dezoito testes aprovados (sete novos de cache e onze de regressão); análise dos três arquivos identificou apenas duas chaves faltantes, corrigidas, e conferência focalizada passou. Build Windows debug com URL oficial e diff aprovados. Checkpoint publicado identificado pela tag atlas-paddock-offline-read-20260926. Próximo marco: agrupar versão e testar consulta/reabertura offline real; não promete edição offline ou atualização automática. É preciso consultar o cadastro uma vez nesta versão para preencher o novo cache; nenhum legado por nomes importado. Aplicativo aberto/celular/banco existente preservados, alteração ainda não instalada neles.

- Pacote concluído (26/09, 100%): versão Windows profile 2 otimizada de avaliação compilada com URL oficial, reunindo guia, motivos e referência de Campo; não é release de distribuição. Usuário confirmou salvar/fechar a anterior; processo ausente conferido antes da abertura. Nova janela PID 17076 responsiva; captura encontrou o usuário já dentro da operação com aviso offline e painel inicial preparando dados, sem autenticação automatizada. Não é prova de carregamento final de todos os módulos. SHA-256 Profile/data/app.so: 30277E5D0C679FD0A52598802803BE1BA8C1FD458EFB4A7CE34395C2EB80AB95. Os 54 testes/análise do checkpoint 0cfcae7 são referência, não repetidos por ausência de mudança de código. Checkpoint publicado identificado pela tag atlas-grazing-grouped-preview2-20260926. Próximo marco: conferir Campo → Piquetes e pastagens → Suporte com dados reais e ensaio offline; Docker/produção continuam sem homologação. Celular e banco existente preservados.

- Pacote concluído localmente (26/09, 100%): snapshot imutável do cadastro oficial de Campo passado pelo atalho e exibido no Suporte com consulta datada, lista e soma nominal válida. Exige ID da fazenda autorizada; IDs vazios/duplicados, áreas inválidas e overflow não viram soma. Sem consulta nova, persistência duplicada ou preenchimento de altura/MS/capacidade. Cinquenta e quatro testes aprovados (cinco novos de snapshot e 49 regressão), análise de cinco arquivos sem problemas, build Windows debug com URL oficial e diff aprovados. Checkpoint publicado identificado pela tag atlas-field-paddock-reference-20260926. Próximo marco: incluir referência e fluxo guiado no release agrupado, depois ensaio operacional. Os dois cadastros não foram migrados/unificados; snapshot é referência datada, não cache persistente ou área efetiva. Aplicativo aberto, celular e banco existente preservados. Homologação PostgreSQL permanece 0% pelo Docker; UA/ha 90%, sincronização da base 85% até testes reais.

- Pacote concluído de diagnóstico/proteções (26/09, 100%): Docker Desktop instalado iniciado sem alterar configurações; backend local apresentou canal, mas log de 16:45 UTC confirmou erro ao inicializar Inference manager no endpoint dockerInference (arquivo não acessível/listener inválido). Consulta pendente interrompida; nenhum contêiner/banco de ensaio criado, homologação PostgreSQL permanece 0%. Executor agora limita info/inspect/pg_isready a dez segundos e limpeza a trinta; identidade divergente na limpeza impede parar outro contêiner e impede anunciar aprovação. Onze testes unitários aprovados, Ruff e compilação Python aprovados; checkpoint publicado identificado pela tag atlas-postgres-probe-timeouts-20260926. Próximo marco: restabelecer Docker Desktop local, sem reset de fábrica automático, para ensaio real até 0057. Aplicativo, celular, produção e banco existente preservados.

- Pacote concluído localmente (26/09, 100% do executor): homologador PostgreSQL descartável implementado para upgrade até 0057 duas vezes, revisão e unicidades company_id/client_operation_id em pesagens/pastejo. Força pipe local, remove contexto Docker externo, usa contêiner/banco aleatórios, porta exclusiva 127.0.0.1, sem montagens do host ou URL externa; limpeza por ID com rótulo próprio conferido. Oito testes de salvaguardas aprovados sem conftest, Ruff e compilação Python aprovados. Docker daemon consultado uma vez e indisponível: nenhuma migração PostgreSQL executada, homologação permanece 0%. Roteiro docs/pilot/HOMOLOGACAO_POSTGRES_ISOLADO.md. Checkpoint publicado identificado pela tag atlas-isolated-postgres-runner-20260926. Próximo marco: Docker Desktop Linux ativo para executar o ensaio real; depois capacidades de produção e teste entre dispositivos. Nenhum build Flutter necessário; banco existente, produção, aplicativo e celular preservados.

- Pacote concluído localmente (26/09, 100%): fluxo guiado com ordem base → animais → pesos, barra 0/3 a 3/3 e ação contextual. Salvar e identificar só continua após gravação/releitura da mesma base; cancelar não avança. Carteira/seleção antigas e conflito da base não viram configuração completa. Conferência usa cache local; texto antigo que dizia não calcular UA/ha corrigido. Quarenta e nove testes aprovados (cinco novos de política/contrato e 44 regressão), análise de três arquivos sem problemas, build Windows debug com URL oficial e diff aprovados. Checkpoint publicado identificado pela tag atlas-grazing-guided-setup-20260926. Próximo marco: agrupar o fluxo no release para avaliação operacional; a versão release aberta e o celular ainda não receberam este pacote. UA/ha segue em 90% e sincronização em 85% até ensaios reais. Banco existente preservado.

- Pacote concluído (26/09, 100%): botão Suporte e cartão visível na lista de Piquetes e pastagens abrem diretamente a aba Suporte da gestão existente; funções CRUD preservadas. O atalho informa ID exato e nome da fazenda, evitando resolver outra fazenda homônima; controlador descartado ao retornar. Quarenta e quatro testes aprovados (contratos de navegação, escopo e regressão), análise de cinco arquivos sem problemas, build Windows release com URL oficial e diff aprovados. Atlas atualizado aberto e responsivo, PID 5936; captura verificou login com PIN offline disponível, sem autenticação automatizada. Inclui diagnóstico individual de pendências. Checkpoint publicado identificado pela tag atlas-field-grazing-support-navigation-20260926. Próximo marco: usuário conferir Campo → Piquetes e pastagens → Suporte com dados reais; UA/ha segue em 90% e sincronização em 85% até homologação. Cadastros legados de piquetes não foram migrados/unificados; celular e banco existente preservados.

- Pacote concluído localmente (26/09, 100%): diagnóstico individual implementado e cartão exibe o motivo específico por animal: inatividade, ausência de peso confirmado no dispositivo, confirmação pendente, antiguidade, dados inválidos/futuros e divergência diária. Múltiplos problemas são preservados, sem extrapolar cobertura. Trinta e oito testes aprovados (dois novos e 36 regressão), análise de três arquivos sem problemas, build Windows debug com URL oficial e diff aprovados. Checkpoint publicado identificado pela tag atlas-grazing-pending-reasons-20260926. Roteiro atualizado; próximo marco: avaliação operacional e incluir diagnóstico no próximo release agrupado. Esta alteração ainda não está na versão profile aberta nem no celular. Banco existente preservado; UA/ha permanece em 90% e sincronização em 85%, pois faltam os ensaios reais.

- Pacote concluído (26/09, 100%): versão Windows agrupada profile (otimizada para avaliação, não release de distribuição) compilada com URL oficial e aberta após o usuário confirmar que salvou/fechou a anterior. PID 9384 responsivo; captura verificou tela de login com entrada offline por PIN disponível, sem autenticação automatizada. Inclui revisão de conflitos, seleção individual e UA/ha do checkpoint a127538. Análise dos três componentes sem problemas; 36 testes do pacote anterior são a referência, não reexecutados porque não houve mudança de código. Roteiro: docs/pilot/VALIDACAO_PASTEJO_UA_WINDOWS_20260926.md. SHA-256 de Profile/data/app.so: 60738BFFD4048D5B1802D09722F7C47E5218E59D9BB07A452077E35986304EA4. Checkpoint publicado identificado pela tag atlas-grazing-grouped-preview-20260926. Próximo marco: ensaio com área/pesos reais e reinício offline, seguido de homologação externa e release de distribuição agrupado. UA/ha permanece em 90% até ensaio operacional; sincronização da base em 85%. Celular e banco existente preservados.

- Pacote concluído localmente (100%, 26/09): calculador e cartão UA/ha implementados com cobertura completa obrigatória, última pesagem confirmada por animal e datas/pendências visíveis. Pesos locais não confirmados, futuros, inválidos ou com mais de 90 dias ficam fora; divergências no mesmo dia bloqueiam o animal por ausência de horário verificável. Leitura usa cache por IDs de empresa/fazenda/animal, sem rede; base, seleção e contexto são reconferidos antes de exibir. Conflito da base e área acima da fazenda impedem o índice; amostra parcial nunca é extrapolada. Trinta e seis testes aprovados (dez novos e 26 regressão), análise dos três arquivos sem apontamentos, build Windows debug com URL oficial e diff aprovados. Checkpoint `a127538` e tag `atlas-grazing-ua-indicator-20260926` publicados. Referência Embrapa: peso total / 450 / área; 90 dias é política operacional do Atlas, não recomendação agronômica. Próximo marco: release agrupado de revisão/seleção/UA e ensaio com pesos reais; consulta das pesagens no Rebanho repovoa o cache confirmado. Release aberto, celular e banco existente preservados. Vínculos/auditoria continuam locais; sincronização da base segue em 85% aguardando homologação externa.

- Pacote concluído localmente (100%, 26/09): carteira isolada por IDs, busca por brinco/nome e seleção de animais ativos implementadas em Pastagens → Suporte → Identificar animais em pastejo. A contagem corresponde à base; novas operações ou valores corrigidos não herdam vínculos anteriores. Seleções são locais, históricas e serializadas; base/carteira precisam estar atuais (até sete dias) para salvar. A carteira é consultada explicitamente online uma vez e fica disponível offline, sem usar cache legado por nomes. Vinte e seis testes aprovados, incluindo sete novos de escopo, reinício, contagem, animais inativos, contexto e concorrência. Análise dos três arquivos sem apontamentos, build Windows debug com URL oficial e diff aprovados. Checkpoint `46841cd` e tag `atlas-grazing-animal-selection-20260926` publicados. UA/ha ainda não calculada e vínculos não sincronizados; próximo marco: pesos datados confiáveis por ID selecionado e cobertura explícita, depois release agrupado. Aplicativo aberto, celular e banco existente preservados. Sincronização da base permanece em 85%, aguardando ensaio externo; essa porcentagem não representa a implantação dos vínculos individuais.

- Pacote concluído localmente (100%, 26/09): revisão assistida com confirmação explícita, aceitação da versão remota sem escrita na API e auditoria local das duas versões com autor/data. A auditoria é gravada antes da substituição; falha posterior permite retomar sem duplicá-la. Conflito obsoleto, mudança de usuário/fazenda e escopo divergente são recusados. Dezenove testes aprovados, análise dos quatro arquivos alterados sem apontamentos, build Windows debug com URL oficial e diff aprovados. Checkpoint `0bdbe96` e tag `atlas-grazing-conflict-review-20260926` publicados. Release aberto e celular preservados; a revisão nova entra no próximo release agrupado. Sincronização de pastejo em 85%; faltam homologação PostgreSQL/produção e ensaio em dois dispositivos. Auditoria das revisões continua local, não sincronizada. Próximo marco externo: ensaio autenticado; próximo trabalho local possível: identificação dos animais em pastejo para futura base de UA/ha, sem inferir peso pelo número de cabeças. Banco existente preservado.

- Pacote concluído localmente (100%, 26/09): adaptador remoto e conciliação manual implementados, com paginação, envio só de operações ausentes, bloqueio após conflito e preservação persistente das duas versões. Pastagens → Suporte ganhou comando e status, sem rede na abertura/gravação. Quinze testes aprovados (sete conciliação, oito persistência). Análise completa executada: quatro avisos de chaves corrigidos; conferência focalizada passou sem apontamentos. Build Windows release com URL HTTPS oficial aprovado; Atlas aberto e responsivo (PID 16172), tela de login verificada por captura de janela, sem preencher autenticação. Checkpoint `f8f0172` e tag `atlas-grazing-sync-client-20260926` publicados. Etapa de sincronização em 70%: faltam revisão assistida dos conflitos, homologação PostgreSQL/produção e ensaio real em dois dispositivos. Limites por comando: até 20 envios e 20 páginas; histórico incompleto não autoriza envio. Celular e banco existente preservados. Próximo marco: revisão supervisionada dos conflitos, mantendo versões auditáveis, seguida de ensaio externo quando disponível.

- Pacote concluído localmente (100%, 26/09): API de base de pastejo com histórico paginado, identidade idempotente por empresa, conflito 409 sem sobrescrita, isolamento de fazenda, autorização nutrition.read/write, confirmação de área única e datas UTC. Capacidade só é positiva com tabela e unicidade presentes; sem migração, leitura/gravação retornam 503. Migração aditiva 0057 e bootstrap SQLite até head aprovados. Treze testes distintos aprovados (cinco API, uma migração, dois bootstrap e cinco regressão de pesagens); ensaio UTC adicional aprovado. Ruff dos arquivos novos/modelo/testes aprovado; rota legada mantém os mesmos 90 apontamentos anteriores. Compilação Python e diff aprovados. Checkpoint `9d744cb` publicado. PostgreSQL e deploy/migração reais não homologados; cliente ainda não envia esta base. Sincronização geral em 40%; próximo marco: adaptador e conciliação do cliente com capacidade confirmada, depois ensaio em dois dispositivos. Sem build Flutter ou instalação, pois o pacote só altera backend; banco existente preservado.

- Pacote concluído localmente (100%, 26/09): histórico de pastagens com identidade estável, gravações serializadas entre instâncias, repetição local sem duplicação e rejeição de alterações divergentes da mesma operação. Armazenamento ilegível ou de outro escopo não é sobrescrito. Oito testes aprovados, inclusive 20 gravações simultâneas e recuperação após erro. Análise completa executada: dois avisos corrigidos; conferência focalizada identificou somente import redundante, removido sem nova análise completa. Testes repetidos após correção aprovados; diff sem erros. Checkpoints publicados `84771f2` e `fc3a3d1`. Sem build neste pacote de persistência Dart, sem alterações nativas. Próximo marco: API autenticada e migração aditiva, depois conciliação remota e teste em dois dispositivos. Sincronização total em 20%; não ativada. Celular e banco `backend/atlas_test.db` preservados.

- Pacote concluído localmente (100%): base manual e rastreável para lotação da pastagem com histórico isolado por empresa, tenant e fazenda; formulário exige área única confirmada e número real de animais em pastejo. O indicador animais/ha aparece em Pastagens e no painel de Corte, separado dos índices por área total, com data e expiração após sete dias; base de outra fazenda/empresa/tenant ou área acima da fazenda não vira índice atual. O painel usa a área decimal exata da fazenda, evitando o arredondamento do cadastro legado. Doze testes Flutter, análise estática, build Windows debug com URL oficial e `git diff --check` aprovados em 25/09. Checkpoints `b181c4f` e `e0a5574`, com tags publicadas. A etapa de produto fica em 90% até ensaio com dados reais e release agrupado; histórico ainda local deste dispositivo. Próximo marco: sincronização entre dispositivos e, depois, UA/ha com pesos. `backend/atlas_test.db` preservado; celular não atualizado neste pacote.

- Pacote concluído localmente (100%): o painel de Pastagens não chama mais a soma dos piquetes de área total/efetiva; alerta para sobreposições e áreas fora de uso, ignora áreas inválidas e pondera a capacidade pela área. Três testes de cálculo, análise estática e build Windows debug com URL oficial aprovados em 25/09; checkpoint `1dbffa6` e tag `atlas-pasture-area-audit-20260925` publicados. O cronograma foi auditado e ganhou uma sequência única de oito passos restantes. Persistência do PIN concluída após confirmação do usuário; a rota de capacidade em produção passou de 404 para 403 sem credencial, mas a migração 0056 e a resposta autenticada continuam sem confirmação. Próximo marco local: entrada explícita da área efetiva e do rebanho em pastejo. `backend/atlas_test.db` preservado.

- Pacote implementado e instalado (98%, ensaio manual pendente): PIN e sessão usam o mesmo modo `encryptedSharedPreferences` no Android; a leitura da sessão deixa de migrar o PIN para um cofre diferente daquele consultado no reinício. A recuperação de sessão não executa mais `deleteAll`, preservando outras chaves. Validações em 25/09: 19 testes de segurança/sessão/Configurações/rede, análise estática sem apontamentos, APK Android release com URL oficial, `adb install -r` bem-sucedido no moto g75 5G sem mudar `firstInstallTime` (18/09). SHA-256 do APK `8E721A6E9A317F7717FBD11399D13D048E72B3D8D858C81CDC4580D55768B22A`; checkpoint `8b32bd6` publicado. Próximo marco: confirmar em Configurações que o PIN continua configurado após fechar/reabrir e testar entrada offline sem tocar em “Sair”. Se o PIN anterior já tiver sido perdido, configurar uma única vez nesta versão. `backend/atlas_test.db` preservado.

- Pacote implementado e instalado (98%, ensaio manual pendente): falha transitória na renovação do token não apaga mais a sessão e a fazenda locais; rejeição explícita da credencial continua invalidando-as. O login diferencia PIN não configurado de sessão/fazenda ausente e apresenta aviso legível no celular. Validações em 25/09: 17 testes de rede/sessão/login aprovados, análise estática sem apontamentos, APK Android release com URL oficial, `adb install -r` bem-sucedido no moto g75 5G, pacote `br.com.projetoatlas.app` atualizado sem mudar `firstInstallTime` (18/09). SHA-256 do APK `2622EBCCB37E63484CFC6DA8FF05B836A207D956FF7E7DB5240403414E792383`; checkpoint `111aa2e` publicado. Se uma versão anterior já apagou a sessão, o usuário precisa entrar online uma vez; depois deve fechar/reabrir sem rede, sem usar “Sair”. Próximo marco: validar a entrada por PIN no aparelho sem internet. `backend/atlas_test.db` preservado.

- Pacote implementado e instalado (98%, validação manual pendente): confirmação divergente mantém o diálogo aberto com erro explícito; o PIN salvo é relido antes de confirmar sucesso. Uma nova sessão relê o PIN existente, evitando convite falso. O diálogo agora descarta seus campos apenas ao sair da árvore visual. Validações em 25/09: 23 testes de sessão/Configurações/login, análise estática sem apontamentos, APK Android release com URL oficial, `adb install -r` bem-sucedido no moto g75 5G (serial ZF524XKRWZ), pacote `br.com.projetoatlas.app` aberto; data da primeira instalação preservada (18/09). SHA-256 do APK `654E48DCE8468F1CC7A7DBCAD540AE346C1E88613F059C711F6E6041378AD986`; checkpoint de código `88970c9`, documentação publicada no checkpoint seguinte. “Sair” ainda remove deliberadamente a sessão local e requer novo login online; para testar PIN, fechar e reabrir sem rede. Falta o usuário cadastrar o PIN no aparelho e confirmar a entrada offline real, sem compartilhar o segredo. `backend/atlas_test.db` preservado.

- Pacote concluído (100%): a renovação em segundo plano consulta a sessão sem persistir antes de confirmar a identidade e a geração do login. Respostas tardias após logout ou nova conta são descartadas, sem recolocar o usuário anterior no dispositivo. Validações em 25/09: dois testes de concorrência novos, 20 testes de sessão/login, análise estática sem problemas, um build Windows debug com URL de produção e `git diff --check`. Checkpoint de código `ad84d95`; documentação publicada no checkpoint seguinte. O banco `backend/atlas_test.db` e versões instaladas foram preservados. Próxima etapa: ensaio de PIN sem internet no dispositivo, homologação de PostgreSQL descartável e confirmação da migração 0056 no Render antes da pesagem real offline→online. A automação foi reduzida a uma execução diária às 09h para entregar pacotes agrupados e limitar validações repetidas.

- Pacote concluído (100%): recarregamento remoto e seleção direta de fazendas reutilizam a verificação de empresa, tenant e permissões já aplicada ao cache. Quatro testes novos cobrem resposta remota mista, recusa de busca direta fora da empresa ou sem permissão e preservação da carteira local na falha da API. Validações em 25/09: 18 testes de sessão/login, análise estática sem problemas, build Windows debug com URL de produção e `git diff --check`. Checkpoint de código `eb0e031`; documentação publicada no checkpoint seguinte. O banco `backend/atlas_test.db` e o release instalado não foram alterados. Próxima etapa: validar PIN sem internet no dispositivo e concluir a homologação isolada PostgreSQL/produção da migração 0056 antes do teste real de pesagem offline→online.

- Pacote concluído (100%): acesso por PIN com contexto local autorizado. No reinício, a carteira em cache é limitada a empresa, tenant e fazendas permitidas; uma fazenda salva de outra empresa é substituída também no contexto global/persistido. O PIN não abre uma sessão sem empresa ou fazenda autorizada. Seis testes novos cobrem reinício offline sem aguardar a API, PIN errado, bloqueio após tentativas e isolamento de empresa/tenant/fazenda. Validações em 25/09: 14 testes Flutter de sessão/login, análise estática sem problemas, build Windows debug com URL de produção e `git diff --check`. Checkpoint de código `0f61d5e`; documentação publicada no checkpoint seguinte. O banco `backend/atlas_test.db` foi preservado; aplicativo em execução e celular não foram atualizados. Próximo marco: testar PIN sem internet no dispositivo, homologar migrações em PostgreSQL descartável quando disponível e confirmar 0056 no Render antes de ensaio real de pesagem offline→online.
- Pacote concluído (100%): integridade dos indicadores ao revisar pesagens divergentes. O histórico conserva a versão local e a remota, mas cartões e variações contam só uma medição por chave, preferindo a confirmação remota; a interface explica a diferença. Validações em 25/09: 27 testes Flutter de pesagens/painel, análise estática sem apontamentos, build Windows debug com URL de produção e `git diff --check`. Checkpoint de código `19f9517`; documentação publicada no checkpoint seguinte. `backend/atlas_test.db` preservado. Próximo marco: homologação PostgreSQL descartável quando o ambiente estiver disponível, confirmação de 0056 no Render, teste real offline→online e release agrupado. Aplicativo aberto e celular não foram atualizados neste pacote.
- Homologação PostgreSQL isolada: pendente de ambiente, 0%. Docker Desktop foi solicitado localmente, mas o daemon não iniciou; nenhuma conexão com produção ou banco do usuário foi tentada. O ensaio exigirá instância descartável e segue no cronograma, sem ser substituído pela aprovação em SQLite.
- Pacote concluído localmente (100%): instalação do backend em banco vazio SQLite. `alembic upgrade head` concluiu da revisão inicial até 0056 em banco temporário e a reexecução foi idempotente; outra regressão preservou e expandiu por fazenda registros legados de onboarding. O núcleo inicial passou a criar apenas as tabelas históricas e as incompatibilidades SQLite comprovadas foram corrigidas. Validações: dois testes isolados de bootstrap, 27 testes de reconciliação/migração de pesagens com `--noconftest`, Ruff, compilação Python e `git diff --check` em 25/09. Checkpoint de código `1e3d250`; documentação no checkpoint seguinte. O banco `backend/atlas_test.db` permaneceu fora do escopo e não foi incluído no commit. Próximas etapas: homologar o histórico em PostgreSQL descartável, confirmar no Render o deploy da migração 0056, testar pesagem real offline→online e agrupar releases Windows/Android. Docker local indisponível nesta auditoria; SQLite não comprova o resultado em PostgreSQL.
- Pacote concluído (100%): revisão supervisionada de pesagens com conflito. A mesma chave de operação só é conciliada automaticamente quando todos os dados da medição conferem; uma divergência conserva as versões local e remota e exige duas confirmações para remover apenas a pendência local. Validações: 27 testes Flutter de pesagens/painel, análise estática sem apontamentos, build Windows debug com URL de produção e `git diff --check` aprovados em 25/09. Checkpoint de código `4a4b440`; documentação publicada no checkpoint seguinte. Próxima etapa: confirmar no Render o deploy do backend e a migração 0056, executar pesagem real offline→online e agrupar release Windows/Android. Até a rota de capacidade estar disponível, a fila não reenviará automaticamente. Banco de teste local do usuário preservado; aplicativo aberto e celular não atualizados neste pacote.
- Situação: isolamento do cache de pesagens concluído e checkpoint `9e29a25` publicado; nenhum release Windows foi reaberto neste pacote (o processo anterior PID 20340 não estava mais ativo na última verificação).
- Etapa atual: homologar o histórico em PostgreSQL isolado e confirmar a publicação da migração 0056 no Render; depois validar pesagem offline→online e PIN sem internet no dispositivo. O isolamento do contexto de PIN foi concluído localmente, mas não substitui o ensaio real. A fila de fotos fiscais não será refeita.
- Progresso: isolamento de cache e fila de pesagens 100% localmente; contrato de servidor em produção não confirmado; isolamento do contexto de PIN 100% localmente; acesso local-primeiro 98% até teste físico sem internet.
- Componentes concluídos neste pacote: cache v2 indexado por IDs de empresa, fazenda e animal; os consumidores informam a fazenda explicitamente. Dados antigos por nome ficam preservados, sem migração automática para outra empresa; leitura confirmada do servidor repovoa o cache isolado. Sem ID de empresa/fazenda, o cache compartilhado não é escrito.
- Validações concluídas: 25 testes Flutter de pesagens e painel técnico, análise estática sem apontamentos, build Windows debug com URL HTTPS de produção e `git diff --check` em 25/09.
- Checkpoint Git publicado: `9e29a25` — `fix(beef): isolate cached weighings by company and farm`.
- Verificação externa: `/health/ready` respondeu HTTP 200 em 0,74 s, mas `/livestock/weight-sync-capabilities` e rota inexistente responderam 404. A API publicada ainda não apresenta o novo contrato. Painel do Render exigiu login no navegador disponível; nenhum segredo ou log privado foi acessado e nenhum deploy foi acionado.
- Próximo marco: quando houver acesso ao painel do Render, verificar o último deploy/migração 0056; até lá, a fila retém pesagens sem reenvio inseguro. Em paralelo, implementar resolução supervisionada dos registros com conflito. O aplicativo e o celular não foram atualizados neste pacote.
- Limitação: o histórico offline antigo sem vínculo verificável à empresa não aparece no cache v2 até ser obtido novamente do servidor. O dado legado não foi apagado; essa escolha impede atribuí-lo silenciosamente a outra conta.
- Componentes concluídos neste pacote: gravação da pesagem na fila local por empresa/fazenda/animal antes do POST, indicação visível de pendência, tentativa após abrir a tela ou registrar, confirmação explícita de capacidade idempotente da API antes de repetir, conciliação pelo `client_operation_id` e bloqueio de repetição após conflito. Falha ou resposta ambígua não apaga a fila; cache remoto não substitui as pendências.
- Validações concluídas: nove testes Flutter (offline, persistência, isolamento da fila, resposta perdida, API antiga, conflito), cinco testes da API em banco temporário, análise estática sem apontamentos, compilação Python e build Windows debug com URL HTTPS de produção em 23/09.
- Checkpoint Git publicado: `37e6923` — `feat(beef): persist and reconcile offline weighings`.
- Próximo marco: verificar o endpoint `/livestock/weight-sync-capabilities` e a migração 0056 em produção, auditar o cache legado entre empresas e desenhar uma ação supervisionada para conflitos; depois consolidar um release Windows para avaliação. O aplicativo aberto e o celular não foram atualizados neste pacote.
- Limite: o endpoint público de saúde 200 não comprova a migração; a fila aguarda confirmação da API antes de qualquer reenvio. A sincronização automática ocorre ao abrir a tela ou após salvar, e há botão manual; ainda não existe um serviço global de sincronização em segundo plano.
- Sondagem externa após a publicação: `/livestock/weight-sync-capabilities` e uma rota inexistente expiraram ambas após 12 s, sem bytes recebidos. Isso não confirma nem refuta o deploy; a verificação da produção continua pendente. Não foram acessadas credenciais.
- Componentes previstos: concluídos — formulário, modelo de pesagem/serialização, payload do serviço de envio, testes Flutter e cronograma.
- Validações concluídas: nova pesagem recebe UUID, mantém o mesmo identificador no mapa local e no POST, resposta remota o preserva; registro legado sem chave continua compatível. Quatro testes Flutter, análise estática e build Windows debug com URL HTTPS de produção aprovados em 23/09.
- Componentes concluídos no pacote anterior: chave `client_operation_id` é gerada uma vez, persistida no objeto local e transmitida ao servidor; novos IDs locais usam UUID v4 em lugar de microssegundos. Naquele checkpoint, nenhuma falha remota era marcada como pesagem salva offline.
- Checkpoint Git publicado do pacote: `2dba4ee` — `feat(beef): attach stable operation ID to new weighings`.
- Próximo marco: confirmar API 0056 em produção e só então implementar fila persistente com estado de envio; próximo release Windows agrupado não interrompe a avaliação atual.
- Limitação de validação: `alembic upgrade head` partindo de SQLite vazio falhou na revisão histórica `20260804_0001` por referência não carregada a `atlas_iot_devices_v2`; a migração nova foi testada isoladamente. Essa falha de bootstrap é anterior ao pacote e terá auditoria própria.
- Componentes concluídos neste pacote: POST de pesagem aceita `client_operation_id` opcional, devolve o mesmo registro em reenvio idêntico, recusa dados divergentes com HTTP 409 e mantém o comportamento dos clientes anteriores. Unicidade por empresa impede duplicação concorrente; a resposta expõe o identificador para conciliação futura.
- Checkpoint Git publicado do pacote: `12f4562` — `feat(beef): make weight creation retry-safe`.
- Próximo marco: confirmar a migração no ambiente de produção sem acessar segredos e implementar persistência local/fila de pesagens somente após o contrato estar ativo.
- Componentes concluídos neste pacote: atalho vindo do painel abre o formulário sobre o histórico local; depois de fechá-lo, atualiza o histórico remoto em segundo plano. A regra de gravação não foi alterada: uma falha no POST remoto não é apresentada como pesagem salva offline.
- Checkpoint Git publicado do pacote: `8b6d14b` — `perf(beef): open weighing form from local history`.
- Próximo marco: incluir o fluxo no próximo release agrupado após avaliação do atual; projetar idempotência e fila segura para gravação sem rede em pacote separado.
- Componentes concluídos neste pacote: cada animal da lista de pesagens pendentes abre diretamente o formulário de nova pesagem no contexto da fazenda e do lote; ao voltar, o painel é atualizado. O fluxo de gravação da tela existente foi reutilizado; não houve alteração de contrato de sincronização nem promessa de registro offline remoto.
- Checkpoint Git publicado do pacote: `4335382` — `feat(beef): open weighing directly from pending list`.
- Próximo marco: incluir o atalho no próximo release agrupado após o usuário avaliar a versão atual; estudar a gravação de pesagens sem rede como pacote separado.
- Checkpoint Git do diagnóstico: tag `atlas-api-disponibilidade-20260923` publicada com a documentação; não houve mudança funcional.
- Próximo marco: coletar horário e duração de uma nova lentidão se ocorrer; o login local-primeiro e o PIN devem permanecer disponíveis sem aguardar a API.
- Componentes concluídos neste pacote: release Windows de produção reúne peso médio do rebanho ativo com cobertura, denominador explícito da área total e relação de animais com pesagem pendente. Aplicativo aberto para avaliação; APK não alterado.
- Próximo marco: obter feedback da janela aberta, testar o PIN sem internet e investigar a disponibilidade remota separadamente caso continue lenta.
- Componentes concluídos neste pacote: cartão “Animais para pesar” com lista navegável de animais ativos sem pesagem válida nos últimos 90 dias; cada item apresenta brinco/nome, lote e data da última pesagem válida ou ausência dela. A lista é carregada sob demanda visual em diálogo rolável e usa a mesma base da contagem do painel.
- Validações concluídas neste pacote: 26 testes de Corte/painel, análise estática sem problemas e build Windows debug com URL HTTPS de produção em 23/09. Banco local do usuário preservado; APK não alterado.
- Checkpoint Git publicado do pacote: `35a75b7` — `feat(beef): list animals overdue for weighing`.
- Próximo marco: conferir a lista com dados reais no próximo release Windows agrupado e testar desbloqueio offline com o PIN cadastrado.
- Componentes concluídos neste pacote: rótulos de animais/ha e kg/ha explicitam que o denominador é a área total cadastrada; cartão mostra essa área e informa que não representa lotação do pasto. Quando ela falta, ambos os índices ficam indisponíveis e surge orientação para completar o cadastro. Valores longos do cartão agora quebram linha no Windows e em telas estreitas.
- Validações concluídas neste pacote: 23 testes de Corte/painel, análise estática sem problemas e build Windows debug com URL HTTPS de produção em 23/09. Banco local do usuário preservado.
- Checkpoint Git publicado do pacote: `c2acc9d` — `fix(beef): clarify total-area density indicators`.
- Componentes concluídos neste pacote: peso médio calculado somente com animais ativos de peso válido; base X/Y visível; peso vivo/ha indisponível quando faltar peso de algum ativo; área não finita rejeitada também no cálculo de Leite. A tela informa o dado que falta sem exibir zero enganoso.
- Validações concluídas neste pacote: 22 testes de Corte/painel, análise estática sem problemas e build Windows debug com URL HTTPS de produção em 23/09. Banco local do usuário preservado.
- Checkpoint Git publicado do pacote: `e605f33` — `fix(beef): validate active herd weight coverage`.
- Componentes concluídos: build Windows release com URL HTTPS de produção, executável atualizado e janela do Atlas.
- Validações concluídas: `scripts/build_atlas_windows_production.ps1` aprovado em 23/09; processo release PID 6704 aberto e responsivo. Banco local do usuário preservado.
- Checkpoint Git do código incluído: `a2d1f5b` (idade na venda), `c400d57` (evolução mensal) e os pacotes de cobertura `f1f6c7c`/`0d2ea0e`.
- Componentes concluídos: indicador de idade média aproximada na venda, tamanho da amostra e alerta de nascimento inválido/ausente.
- Validações concluídas: 18 testes de Corte/painel, análise estática sem problemas e build Windows debug com URL de produção; casos de nascimento válido, data impossível, venda anterior ao nascimento e janela anual cobertos.
- Checkpoint Git: `a2d1f5b` — `feat(beef): report verified age at sale`.
- Componentes concluídos: agregador mensal de pesos por última medição válida de cada animal, serviço do painel e cartão de evolução com média e tamanho da amostra; série histórica geral inclui animais que integraram o rebanho naquele mês, enquanto a cobertura atual permanece restrita aos ativos.
- Validações concluídas: 16 testes de Corte/painel, análise estática sem problemas e build Windows debug com URL de produção. Repetição do mesmo animal, pesos zero/negativos/não finitos, datas futuras e mês vazio cobertos.
- Checkpoint Git: `c400d57` — `feat(beef): show reliable monthly weight evolution`.
- Componentes concluídos: modelo de cobertura, cartão do número de animais ativos sem pesagem recente e alerta de baixa cobertura com o déficit da amostra.
- Validações concluídas: 13 testes de Corte/painel, análise estática sem problemas e build Windows debug com URL de produção; contagens verificadas para cobertura parcial, total e sem ativos.
- Checkpoint Git: `0d2ea0e` — `feat(beef): show animals missing recent weights`.
- Componentes concluídos: calculador de cobertura, rótulos e alertas do painel de Corte; a data da última pesagem válida permanece independente da janela de cobertura.
- Validações concluídas: 13 testes de Corte/painel, análise estática sem problemas e build Windows debug com URL de produção. A pesagem no 90º dia conta, a de 91 dias não; a virada do mês não zera a cobertura.
- Checkpoint Git: `f1f6c7c` — `fix(beef): use rolling 90-day weighing coverage`.
- Componentes concluídos: calculador de cobertura de pesagens de animais ativos, análise e painel técnico de Corte; série histórica geral preservada.
- Validações concluídas: 11 testes de Corte/painel passaram; `flutter analyze` dos seis arquivos envolvidos sem problemas; builds Windows debug e release com URL de produção aprovados. O release atualizado foi reaberto em 23/09 (PID 4252, janela responsiva). Casos de animal vendido mais recente, duplicidade, datas futuras, peso inválido e ausência de rebanho cobertos.
- Checkpoint Git: `2c61932` — `fix(beef): scope latest weighing coverage to active animals`.
- Componentes verificados neste pacote: `scripts/build_atlas_windows_production.ps1`, build Windows release com `ATLAS_ENV=production` e `ATLAS_API_BASE_URL=https://atlas-api-29y2.onrender.com/api/v1`, e janela `projeto_atlas` de produção.
- Validações concluídas neste pacote: endpoint `/api/v1/health/ready` retornou HTTP 200; `flutter build windows --release` concluiu; processo release PID 3380 aberto e responsivo. A execução debug anterior usava `127.0.0.1:8000` por não receber `dart-define`, o que explica a falha de conexão mostrada pelo usuário.
- Validação restante de acesso: testar o desbloqueio com o PIN cadastrado quando o dispositivo estiver sem internet; a autenticação online e a criação do PIN já foram confirmadas pelo usuário.
- Contexto da validação anterior: o APK Android atual foi gerado e os testes do cálculo passaram. A compilação Windows estava lenta, mas concluiu após a geração do snapshot Dart; não havia falha do MSBuild.
- Componentes implementados: GMD calculado com primeira e última pesagem válida de cada animal ativo nos últimos 12 meses; painel informa quantos animais têm pares válidos e sinaliza amostra pequena; parser rejeita datas impossíveis.
- Validações concluídas: testes de pareamento, exclusão de dados inválidos, contrato do painel e APK Android debug aprovado (SHA-256 `01E0CA243AA48DE987C7486B9894A99AF31956B653439B8D19AD72D8A7C93226`).
- Checkpoint Git publicado: `d24dc6f` — `fix(beef): calculate gain from paired animal weights`.
- Validação restante: conferir no painel de Corte, com sessão da fazenda, os números e alertas apresentados para pesagens reais.
- Próximo marco: validar desbloqueio offline pelo usuário e conferir os indicadores de Corte com dados reais na janela aberta; após feedback, definir a próxima lacuna operacional.

## Próximo pacote planejado

Conferir, em Produção de corte, a evolução mensal, a idade na venda, o GMD e a cobertura de pesagens com dados reais. Validar uma abertura offline com o PIN cadastrado. O OCR local depende de teste com nota real no aparelho.

## Como acompanhar

Em cada pacote, este arquivo será atualizado com:

1. porcentagem real da etapa atual;
2. arquivos e componentes em alteração;
3. validações já executadas e pendentes;
4. checkpoint Git publicado;
5. próximo marco objetivo.

Uma etapa só é marcada como concluída depois de análise/teste aplicável, checkpoint Git e publicação. Mudanças visuais também exigem build Windows confirmado.
