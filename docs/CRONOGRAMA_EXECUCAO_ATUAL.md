# Cronograma de execução — Atlas

Atualizado em 25/09/2026. Este arquivo é a fonte visível de acompanhamento dos pacotes em execução.

| Etapa | Situação | Progresso | Critério de aceite |
|---|---:|---:|---|
| Acesso: persistência do PIN no Android | Concluído | 100% | PIN e sessão usam o mesmo `EncryptedSharedPreferences`; recuperação da sessão não apaga todas as chaves do cofre. 19 testes, análise estática e APK release aprovados em 25/09; atualização por cima no moto g75 5G preservou a instalação e dados; checkpoint `8b32bd6` publicado. O usuário confirmou que o PIN passou a persistir em 25/09. O ensaio completo sem internet permanece nas etapas específicas de entrada offline. |
| Acesso: preservar sessão offline na falha de renovação | Em validação no aparelho | 98% | Timeout, rede indisponível e falha transitória da API não removem a sessão local; rejeição explícita ainda encerra credenciais inválidas. O login diferencia PIN salvo de sessão/fazenda ausente e usa aviso compacto no celular. 17 testes de rede/sessão/login, análise estática e APK Android release aprovados em 25/09; instalado por cima no moto g75 5G, preservando dados; checkpoint `111aa2e` publicado. Falta ensaio manual offline; se a sessão já foi removida pela versão anterior, entrar online uma vez antes do teste. |
| Acesso: configuração e reconhecimento do PIN no Android | Em validação no aparelho | 98% | Confirmação divergente não fecha mais o diálogo; leitura posterior confirma o PIN antes da mensagem de sucesso; novo login relê o estado armazenado. 23 testes, análise estática e APK release aprovados em 25/09; atualização por cima no moto g75 5G com dados preservados, checkpoint `88970c9`. Falta o teste manual de cadastramento/reabertura offline com o PIN do usuário. “Sair” encerra a sessão local e requer login online. |
| Acesso: resposta tardia da renovação de sessão | Concluído localmente | 100% | Renovação em segundo plano não persiste resposta antes de conferir a geração do login e identidade da conta; resposta após logout ou novo login é descartada. Dois testes de concorrência novos, 20 testes de sessão/login, análise estática e um build Windows debug aprovados em 25/09; checkpoint `ad84d95`. Teste real sem internet e release agrupado continuam pendentes. |
| Acesso: carteira remota restrita à sessão | Concluído localmente | 100% | Atualização da lista e busca direta de fazenda aplicam empresa, tenant e permissões; falha de rede não apaga a carteira local. Quatro testes novos, 18 testes de sessão/login, análise estática e build Windows debug aprovados em 25/09; checkpoint `eb0e031`. Release visível agrupado e ensaio real do PIN continuam pendentes. |
| Acesso: contexto autorizado para PIN offline | Concluído localmente | 100% | Cache de fazendas restrito a empresa, tenant e permissão da sessão; fazenda salva inválida corrigida no controlador e contexto global. Seis testes novos, 14 testes de sessão/login, análise estática e build Windows debug aprovados em 25/09; checkpoint `0f61d5e`. O ensaio real sem internet permanece na etapa de entrada local-primeiro. |
| Produção técnica: fundação Corte e Leite | Concluído | 100% | Perfil produtivo persistente, menu condicionado à fazenda, produção diária offline de leite, estado diário do lote, DEL e concepção rastreáveis, e indicadores de Corte para lotação, peso vivo/hectare e ganho médio diário. Cálculos não exibem estimativas sem base de dados. |
| Produção técnica: reprodução de leite rastreável | Concluído | 100% | Painel técnico inclui DEL, concepção, período seco, período de serviço, taxa de prenhez por último diagnóstico e idade ao primeiro parto. O estado do lote registra perdas gestacionais como percentual de gestações acompanhadas, com validação de consistência. Três testes, análise e build Windows aprovados. |
| Produção técnica: cobertura de reposição | Concluído | 100% | O painel mostra a cobertura de reposição dos últimos 12 meses como entradas de fêmeas sobre saídas registradas, separa descartes reprodutivos e informa as contagens de origem. Sem saídas datadas, o sistema declara a ausência de base em vez de produzir uma taxa. Teste, análise e build Windows aprovados. |
| Produção técnica: qualidade temporal do Leite | Concluído | 100% | Inseminações e diagnósticos com data impossível, futura ou fora da janela anual ficam fora de concepção e prenhez. O painel de qualidade de dados explica ao produtor os registros que precisam ser corrigidos. Testes de Leite e Corte aprovados; checkpoint publicado: `2afc75c`. |
| Produção técnica: qualidade da produção diária de Leite | Concluído | 100% | Ordenhas futuras, duplicadas, negativas ou sem vacas ordenhadas não entram em litros/dia, L/ha ou L/vaca. O módulo mostra as lacunas e exige ao menos uma vaca ordenhada nos novos registros. Testes aprovados; checkpoint publicado: `90dd7bc`. |
| Produção técnica: alertas operacionais unificados | Concluído | 100% | O painel técnico de Leite ou Corte concentra no topo os dados a revisar, informa a quantidade de itens e preserva todos os alertas antes dispersos nos cartões de indicadores. Contrato visual e testes de indicadores aprovados; checkpoint publicado: `4a77289`. |
| Produção técnica: cobertura diária do Leite | Concluído | 100% | O módulo e o painel técnico mostram cobertura válida de 30 dias, percentual de regularidade e dias ainda sem ordenha. O alerta informa objetivamente quanto falta para a base mínima de 20 dias. Testes de cálculo e contrato de painel aprovados; checkpoint publicado: `d60b561`. |
| Produção técnica: precisão por vaca ordenhada | Concluído | 100% | L/vaca ordenhada é ponderado pelas vacas efetivamente ordenhadas em cada dia válido; L/vaca em lactação permanece separado e vinculado ao estado mais recente do lote. A média de vacas ordenhadas torna a leitura auditável. Testes e contrato de painel aprovados; checkpoint publicado: `a8e84b1`. |
| Produção técnica: rastreabilidade de pesagens do Corte | Concluído | 100% | O GMD informa quantos animais compõem a primeira e a última pesagem e mostra a cobertura da última amostra. O painel alerta quando faltam duas medições ou a cobertura é inferior a 50% do rebanho ativo. Contrato e testes de indicadores aprovados; checkpoint publicado: `97a524f`. |
| Produção técnica: atualidade dos dados de Leite | Concluído | 100% | A última ordenha válida exibe data e idade, com alerta após três dias. O painel técnico mostra quando o estado de lote está ausente ou tem mais de sete dias, antes que índices por vaca sejam interpretados como atuais. Testes e contrato de painel aprovados; checkpoint publicado: `50b0e9f`. |
| Produção técnica: atualidade das pesagens de Corte | Concluído | 100% | A série preserva a data real da última pesagem, exclui registros futuros e informa data, idade e cobertura da amostra. Após 90 dias, o painel alerta antes do uso de GMD e peso por hectare como retrato atual. Contratos, testes e APK Android atual aprovados; checkpoint publicado: `f3fc0e0`. |
| Corte: GMD pareado por animal | Em validação | 99% | O ganho médio diário usa duas pesagens válidas do mesmo animal ativo nos últimos 12 meses; o painel informa quantos animais compõem a média e alerta quando a cobertura é pequena. Testes de cálculo, APK Android e checkpoint `d24dc6f` aprovados. Build Windows debug concluído em 23/09 com 0 avisos e 0 erros; Atlas aberto com os arquivos atualizados. Resta conferir os números no painel com uma sessão da fazenda. |
| Corte: cobertura da última pesagem dos ativos | Concluído | 100% | A cobertura e a atualidade são calculadas apenas a partir de pesagens válidas dos animais atualmente ativos. Saídas e óbitos não deslocam a data da última amostra; duplicidades não inflam a contagem. A série histórica geral permanece inalterada. Onze testes, análise estática e builds Windows debug/release com URL de produção aprovados; o release atualizado foi reaberto em 23/09. Checkpoint `2c61932`. |
| Corte: cobertura de pesagens em janela móvel | Concluído | 100% | A cobertura de animais ativos passou a usar os últimos 90 dias corridos, sem queda artificial na virada do mês; o 90º dia é incluído e o 91º fica fora. A data da última pesagem permanece visível mesmo quando a cobertura recente é zero. Treze testes, análise estática e build Windows debug com URL de produção aprovados; checkpoint `f1f6c7c`. |
| Corte: ação sobre pesagens pendentes | Concluído | 100% | O painel agora mostra quantos animais ativos não têm pesagem válida nos últimos 90 dias; o alerta de cobertura inferior a 50% informa o déficit exato, sem esconder cobertura total ou ausência de ativos. Treze testes, análise estática e build Windows debug com URL de produção aprovados; checkpoint `0d2ea0e`. |
| Corte: evolução mensal confiável do peso | Concluído | 100% | O gráfico de Corte mostra a média da última pesagem válida de cada animal no mês, com tamanho da amostra; medições repetidas do mesmo indivíduo não pesam mais na média. Pesos inválidos e datas futuras ficam fora, e meses sem base não são inventados. Dezesseis testes, análise estática e build Windows debug com URL de produção aprovados; checkpoint `c400d57`. |
| Corte: idade verificável na venda | Concluído | 100% | O painel calcula idade média aproximada em meses apenas para vendas datadas nos últimos 12 meses com nascimento válido anterior à saída. Mostra a quantidade de vendas que compõem a média e alerta para lacunas, sem chamar toda venda de abate. Dezoito testes, análise estática e build Windows debug com URL de produção aprovados; checkpoint `a2d1f5b`. |
| Corte: release Windows consolidado para avaliação | Concluído | 100% | A versão de produção reúne GMD pareado, cobertura de pesagens em 90 dias, pendências de pesagem, evolução mensal por animal e idade média na venda. Build release com URL HTTPS oficial aprovado em 23/09; aplicativo aberto e janela responsiva (PID 6704). Resta avaliação dos dados da fazenda pelo usuário. |
| Corte: integridade do peso do rebanho ativo | Concluído | 100% | Peso médio usa apenas ativos com peso válido, mostra a base X/Y e não confunde ausência de peso com 0 kg. Peso vivo/ha exige peso válido de todos os ativos e área finita positiva; vendidos e mortos não distorcem o retrato atual. 22 testes, análise estática e build Windows debug com URL de produção aprovados em 23/09; checkpoint `e605f33`. A atualização visível fica para o próximo release agrupado. |
| Corte: base de área explícita | Concluído | 100% | Animais/ha e kg/ha identificam a área total cadastrada e deixam claro que não são lotação de pastagem; área ausente ou inválida não gera índice fictício. Cartão mostra a área usada e acomoda valores longos em telas estreitas. 23 testes, análise estática e build Windows debug com URL de produção aprovados em 23/09; checkpoint `c2acc9d`. A atualização visível fica para o próximo release agrupado. |
| Corte: relação operacional de pesagens pendentes | Concluído | 100% | O painel abre lista rolável dos animais ativos sem pesagem válida nos últimos 90 dias, com brinco/nome, lote e última data válida. A lista e a contagem usam o mesmo conjunto; vendidos, pesos inválidos e datas futuras ficam fora. 26 testes, análise estática e build Windows debug com URL de produção aprovados em 23/09; checkpoint `35a75b7`. Avaliação visual fica para o próximo release agrupado. |
| Corte: ação direta de pesagem pendente | Concluído | 100% | Ao tocar um animal da lista, o Atlas reutiliza a tela de pesagens no contexto da fazenda/lote e abre o formulário de nova pesagem; ao retornar, recarrega os indicadores. O fluxo de gravação existente não foi alterado e registro offline remoto ainda não foi prometido. 26 testes, análise estática e build Windows debug com URL de produção aprovados em 23/09; checkpoint `4335382`. O atalho entra no próximo release agrupado. |
| Corte: abertura local-primeiro do formulário de pesagem | Concluído | 100% | O atalho lê o histórico local e mostra o formulário sem esperar a API; após o fechamento, atualiza o histórico remoto em segundo plano. Teste de interface confirmou ausência de chamada remota antes do formulário, regressão de Corte/painel, análise e build Windows debug com URL de produção aprovados em 23/09; checkpoint `8b6d14b`. Gravação offline persistente continua separada. |
| Corte: gravação de pesagens sem rede | Concluído localmente | 100% | Fila persistente por empresa/fazenda/animal salva antes do POST, exibe pendências, reconcilia respostas perdidas por `client_operation_id` e bloqueia reenvio após 409. Só envia quando a API confirma suporte idempotente; sem confirmação ou rede, mantém a pesagem no dispositivo. Nove testes Flutter, cinco de API, análise, compilação Python e build Windows debug aprovados; checkpoint publicado `37e6923`. A ativação em produção depende de confirmar deploy/migração; release visível será agrupado. |
| Corte: idempotência de novas pesagens na API | Concluído localmente | 100% | `client_operation_id` opcional é único por empresa: reenvio idêntico retorna o registro original, alteração de dados ou animal com a mesma chave retorna 409, e clientes legados seguem funcionando. Migração `20260923_0056`, 5 testes novos, 6 de regressão e compilação Python aprovados em banco temporário; checkpoint `12f4562`. Publicação em produção ainda precisa ser confirmada. |
| Corte: identificador estável da pesagem no aplicativo | Concluído | 100% | Nova pesagem recebe UUID v4 uma vez, o preserva no armazenamento local e envia `client_operation_id` ao servidor; respostas remotas mantêm a referência e registros legados seguem compatíveis. Quatro testes Flutter, análise estática e build Windows debug com URL de produção aprovados em 23/09; checkpoint `2dba4ee`. A fila offline foi entregue no pacote seguinte. |
| Corte: isolamento do cache de pesagens | Concluído localmente | 100% | Cache v2 usa IDs de empresa/fazenda/animal em todos os consumidores, impede exibição cruzada ao trocar de conta e não migra automaticamente histórico antigo sem proprietário verificável. Uma leitura confirmada do servidor repovoa o cache isolado. 25 testes Flutter, análise e build Windows debug aprovados em 25/09; checkpoint publicado `9e29a25`. Legado preservado, porém indisponível offline até reconexão. |
| Corte: revisão supervisionada de pesagens em conflito | Concluído localmente | 100% | A conciliação só remove a fila quando data, peso, ECC, origem, equipamento e observação conferem. Se o servidor devolve a mesma chave com dados divergentes, ambas as versões permanecem visíveis e a pendência só pode ser removida após duas confirmações, sem alterar o servidor ou repetir POST. 27 testes Flutter, análise estática e build Windows debug aprovados em 25/09; código `4a4b440`. O contrato de produção ainda precisa de publicação e teste real offline→online. |
| Corte: indicadores sem duplicar conflito de pesagem | Concluído localmente | 100% | Duas versões da mesma operação continuam visíveis para auditoria, mas peso atual, menor/maior peso e variação usam só a versão remota confirmada. A cópia local divergente não infla os cartões e a tela explica isso. Teste de interface antes/depois da revisão, 27 testes de regressão, análise estática e build Windows debug aprovados em 25/09; checkpoint `19f9517`. Release visível será agrupado. |
| Corte: ativar contrato de sincronização em produção | Em homologação externa | 40% | Em 25/09, `/health/ready` respondeu 200 com PostgreSQL pronto e `/livestock/weight-sync-capabilities` respondeu 403 sem autenticação, em vez do antigo 404: a rota está publicada e protegida. Ainda não há prova da revisão Alembic 0056 nem resposta autenticada da capacidade. Até isso ser confirmado, a fila não reenvia pesagens incertas. Depois validar pesagem real offline→online e agrupar release Windows/Android. |
| Backend: bootstrap de banco vazio SQLite | Concluído localmente | 100% | A revisão inicial cria apenas o núcleo histórico, sem antecipar tabelas futuras; a sequência até 0056 roda e pode ser repetida em banco temporário. Foram corrigidas incompatibilidades SQLite comprovadas na reconciliação e em migrações antigas, sem mudar o ramo PostgreSQL de produção. Dois testes isolados cobrem instalação/reexecução e preservação do onboarding legado; mais 27 testes de reconciliação/pesagens, Ruff e compilação Python aprovados em 25/09. Checkpoint `1e3d250`; `backend/atlas_test.db` preservado. |
| Backend: homologar bootstrap PostgreSQL | Pendente de ambiente isolado | 0% | Executar o histórico completo em instância PostgreSQL vazia e descartável, sem usar o banco de produção nem credenciais do OCR. Docker Desktop foi solicitado em 25/09, mas o daemon permaneceu indisponível; não inferir que a aprovação em SQLite prova o bootstrap em PostgreSQL. |
| Corte: release Windows agrupado para avaliação | Concluído | 100% | Build release com URL HTTPS de produção reuniu integridade do peso, base explícita de área total e lista de pesagens pendentes. Artefato `data/app.so` atualizado em 23/09 às 10:15; após autorização do usuário, versão anterior fechada e nova janela PID 20340 aberta e responsiva. O endpoint externo `/health/ready` não respondeu em 70 segundos e precisa de diagnóstico separado; os dados locais foram preservados. |
| Acesso: diagnóstico pontual da disponibilidade da API | Concluído | 100% | Após uma tentativa de `/health/ready` expirar em 70 s, duas novas sondagens retornaram HTTP 200 em 0,63 s e 0,97 s; `/health/live` respondeu 200 em 0,78 s e o HTTPS de referência em 0,67 s. DNS/TLS estavam rápidos no momento das respostas. Resultado indica intermitência, sem provar a causa; não houve alteração de credenciais nem bloqueio do aplicativo local. Próxima ocorrência deve ser registrada com horário/duração. Checkpoint `atlas-api-disponibilidade-20260923`. |
| Corte: lotação da área efetiva de pastagem | Em validação local | 90% | Além de distinguir soma nominal dos piquetes, o produtor pode confirmar a área única e o número de animais realmente em pastejo. O Atlas guarda histórico local isolado por empresa/tenant/fazenda e exibe animais/ha em Pastagens e Corte, separado da área total; confirmação vence após sete dias e área acima do cadastro decimal exato da fazenda não gera índice atual. Doze testes, análise e build Windows debug aprovados em 25/09; checkpoints `b181c4f` e `e0a5574` publicados. Faltam ensaio com dados reais e release agrupado. UA/ha exige pesos confiáveis e fica separado. |
| Pastagens: compartilhar base efetiva entre dispositivos | Preparação local concluída | 20% | Histórico com ID estável por operação, gravações locais serializadas e repetição sem duplicação; divergências e armazenamento ilegível não são sobrescritos. Oito testes aprovados, análise executada e avisos corrigidos, diff aprovado; checkpoints publicados `84771f2` e `fc3a3d1`. Ainda local: faltam API autenticada, migração, conciliação remota e teste em dois aparelhos. Não copiar registros entre contas por nome da fazenda. |
| Fundação técnica: perfil produtivo da fazenda | Concluído | 100% | Cadastro remoto e local identificam Corte, Leite ou Misto, com sistema produtivo opcional e compatibilidade segura para fazendas anteriores. API, migração Alembic, três contratos Flutter, três testes de servidor, análise estática e build Windows de produção aprovados. |
| Financeiro: OCR, galeria e lançamento offline | Concluído | 100% | Checkpoints `f932fc1` até `614a35a`, análise estática e builds Windows concluídos. |
| Financeiro: fila persistente de fotos offline | Concluído | 100% | A foto é copiada ao armazenamento interno, vinculada ao lançamento local, recebe o id remoto após a confirmação e só é apagada após upload confirmado. Falhas preservam arquivo e vínculo para a próxima tentativa; contrato e build Windows validados. |
| Financeiro: anexos de lançamentos existentes sem conexão | Concluído | 100% | Ao anexar foto a um lançamento já existente sem rede, o Centro de documentos preserva cópia local, mantém o vínculo ao lançamento e sincroniza no próximo carregamento conectado. A interface informa o comprovante pendente sem bloquear a conferência humana. |
| Financeiro: visibilidade de sincronização de comprovantes | Concluído | 100% | O Financeiro informa separadamente os comprovantes preservados no dispositivo e pendentes de envio, sem ocultar o status dos lançamentos. Análise, teste de contrato e build Windows aprovados. |
| Financeiro: compatibilidade MIME para OCR no Windows | Concluído | 100% | O upload multipart identifica JPEG, PNG, WebP e GIF pelo arquivo local, evitando rejeição de fotos como `application/octet-stream`. Dependência declarada, análise, contrato e build Windows aprovados. |
| Financeiro: diagnóstico seguro do provedor OCR | Concluído | 100% | O servidor traduz erros de chave, limite e modelo sem registrar foto, resposta bruta ou segredo. A leitura real confirmou o diagnóstico de limite do provedor. |
| Financeiro: repetir leitura da mesma foto | Concluído | 100% | Se o OCR falhar temporariamente, o usuário pode repetir a leitura com a mesma imagem ou preencher manualmente, sem nova captura. Análise, contrato e build Windows aprovados. |
| Financeiro: prevenção de repetição sem resultado | Concluído | 100% | Quando a chave, modelo ou limite do OCR impedirem a leitura, o aplicativo explica a situação e não oferece uma nova chamada inútil. Análise, contrato e build Windows aprovados. |
| Financeiro: OCR local no celular | Em validação | 98% | Android/iOS leem texto e chave de acesso NF-e/QR no próprio aparelho antes de qualquer opção de nuvem; fornecedor, data, documento, valor e categoria são sugestões revisáveis. A foto e o texto não saem do dispositivo. A leitura agora normaliza data curta de DANFE e rejeita data impossível. Análise estática, cinco testes e APK Android atualizado aprovados; falta somente teste em aparelho com nota real, guiado por `docs/pilot/VALIDACAO_OCR_LOCAL_ANDROID_20260915.md`. |
| Cotações: importação tolerante de retornos XLSX | Concluído | 100% | O importador aceita aba com outro nome e cabeçalhos usuais, permite fornecedor ausente com identificação provisória e usa total declarado quando os itens estiverem incompletos. Nenhum dado é gravado sem revisão; nove cenários automatizados aprovados. |
| Anotações: pastas com confirmação de persistência | Concluído | 100% | A pasta é criada somente após releitura da fonte oficial; a interface distingue sessão expirada, permissão insuficiente, assunto duplicado e indisponibilidade. Testes e análise estática aprovados; homologação manual confirmou a pasta persistida em 15/09. |
| Regressão transversal: Financeiro e acesso offline | Concluído | 100% | 26 testes cobriram Financeiro, OCR local, cotações, login offline, sessão e fila de sincronização. Build Windows de produção e APK Android atual foram gerados sem erros. |
| Arquitetura de Inteligência e Analytics | Concluído | 100% | Central única de Análises definida e protegida por contrato: resumo, diagnóstico, análise por área, cenários e decisões. Copilot e motores especializados permanecem contextuais, sem virar menu concorrente. |
| Inteligência: decisão vinculada a Anotações e Agenda | Concluído | 100% | Uma decisão gera anotação com referência persistente à recomendação e a anotação aproveita a promoção idempotente já existente para a Agenda. Frontend, rota, modelo, migração e contratos passaram. Em 15/09, a recomendação real “Manter acompanhamento da operação” foi salva pela Central e confirmada na lista de Anotações da Fazenda Atlas Produção como “Decisão vinculada à análise”, sem criação duplicada. |
| Anotações: compromisso com data prevista na Agenda | Concluído | 100% | A promoção de uma anotação passou a exigir escolha explícita de data prevista, já enviada ao contrato `due_at` do servidor. Análise estática e dois contratos Flutter aprovados; build Windows de produção concluído com êxito em 15/09. |
| Inteligência: snapshot único por fazenda | Concluído | 100% | A Central guarda um contexto oficial por atualização e envia seu identificador às prioridades e aos cenários. O servidor valida empresa e fazenda antes de reutilizar o snapshot, evitando leituras cruzadas ou dados de momentos diferentes. Contratos Flutter e servidor aprovados; em 14/09 a Central carregou uma prioridade real em sessão autenticada no Windows, confirmando o fluxo completo. |
| Android: atualização da cadeia de build | Concluído | 100% | Gradle 8.14, Android Gradle Plugin 8.11.1 e Kotlin 2.2.20. APK debug recompilado com sucesso pela cadeia atualizada e checkpoint Git auditável. |
| Regressão integrada: Inteligência, Anotações e documentos financeiros | Concluído | 100% | Três contratos Flutter e três testes de servidor aprovados: decisão rastreável, snapshot por fazenda e comprovantes financeiros offline. |
| Produção: auditoria de publicação e disponibilidade | Concluído | 100% | Render está configurado para auto-deploy no branch `master` e publicou `2121e91`. O startup aprovou migrations e schema; a rota de saúde respondeu 200 em 1,02 s e a rota de Inteligência respondeu 403 sem credenciais, confirmando a proteção. O primeiro acesso pode exceder 50 s porque o plano Free hiberna a instância. |
| Corte: resultado comercial e qualidade de dados | Concluído | 100% | Painel técnico calcula receita de saídas comerciais, valor médio por cabeça, preço por kg e mortalidade datada com causa, mantendo registros sem data ou valor fora dos denominadores. |
| Governança: perfis de acesso compreensíveis | Concluído | 100% | Configurações traduz permissões técnicas em atividades operacionais e explica que o menu é filtrado conforme a função do colaborador, sem remover nenhuma autorização existente. |
| Assinaturas: contrato dos planos Atlas | Concluído | 100% | API expõe a assinatura efetiva da empresa e catálogo canônico para Essencial com créditos, Profissional com dados ilimitados e Consultoria com consultoria e gestão de equipes. A tela Configurações apresenta capacidades do plano sem bloquear o modo offline. |
| Histórico animal: geração de PDF observável | Concluído | 100% | A exportação informa início, evita duplo acionamento, exibe progresso no botão e diferencia erro de geração de falha ao abrir um arquivo já salvo. Teste, análise e build Windows de produção aprovados. |
| Corte: qualidade de dados comerciais e sanitários | Concluído | 100% | Painel identifica vendas sem data, valor ou peso, e óbitos sem data ou causa. Datas impossíveis são tratadas como inválidas, sem normalização silenciosa para outro mês. Indicadores usam apenas a base válida e não escondem os registros que precisam ser complementados. Testes aprovados; checkpoint publicado: `72d1b4a`. |
| Acesso: entrada local-primeiro | Em validação | 98% | A disponibilidade do servidor deixou de bloquear o formulário. Após autenticação, a carteira local da mesma empresa abre imediatamente e a atualização de sessão/fazendas ocorre em segundo plano; se falhar, o Atlas permanece funcional no modo offline protegido por PIN. O usuário confirmou login mais rápido e configurou o PIN em 23/09; regressão automatizada do PIN e isolamento do contexto aprovados em 25/09 (`0f61d5e`). Resta testar explicitamente uma abertura sem internet com o PIN no dispositivo. |
| Acesso Windows: configuração de produção | Concluído | 100% | A janela debug aberta para avaliação em 23/09 apontava por padrão para `127.0.0.1:8000` e não podia alcançar o Render. Foi encerrada e substituída por build Windows release com a URL HTTPS oficial. O endpoint de prontidão respondeu HTTP 200 e o executável de produção está aberto e responsivo; o login real depende das credenciais do usuário. |
| Acesso: offline explícito e seguro | Concluído | 100% | O login diferencia autenticação remota de entrada local: quando sessão, fazenda e PIN existirem, “Entrar offline com PIN” aparece antes do formulário. Sem contexto completo, a interface explica corretamente que e-mail e senha precisam do servidor. |
| Acesso: ativação guiada do PIN offline | Concluído | 100% | Após entrada autenticada sem PIN, a operação exibe um convite dispensável que abre Configurações. O aviso adapta seus controles a telas estreitas, sem interromper o trabalho da fazenda. Contratos de sessão e login aprovados; checkpoint publicado: `1506a99`. |
| Publicação do OCR no Render | Bloqueado externamente | 90% | O servidor e a chave estão ativos, mas a API de leitura retornou limite atingido. A homologação final depende da renovação/ampliação da cota do provedor. |

## Sequência restante até a homologação geral

1. **Entrada offline no aparelho:** com sessão e fazenda locais, fechar o Atlas sem usar “Sair”, desligar a internet, abrir com o PIN e confirmar que os módulos locais permanecem acessíveis. Isto fecha as três etapas de acesso ainda em 98%; a persistência isolada do PIN já foi confirmada.
2. **Indicadores de Corte com dados reais:** conferir no painel de uma fazenda duas pesagens válidas do mesmo animal, amostra e GMD pareado. Os cálculos automatizados já passaram; falta o aceite com dados da operação.
3. **Base efetiva de pastagem:** a entrada manual confirmada e animais/ha já foram implementados localmente em Pastagens e Corte. Faltam validar com dados reais, publicar em release agrupado e sincronizar o histórico entre dispositivos; UA/ha só após uma base confiável de pesos dos animais em pastejo. A soma nominal dos piquetes permanece separada.
4. **Migração em ambiente isolado:** executar todas as revisões Alembic até 0056 em PostgreSQL vazio e descartável; o ensaio SQLite não substitui esta prova. Depende de um serviço PostgreSQL local/descartável funcional.
5. **Contrato de pesagens em produção:** confirmar no Render a revisão 0056 e obter resposta autenticada da rota de capacidade. O 403 sem credencial prova proteção/rota publicada, não prova banco migrado nem idempotência operacional. A fila permanece segura sem reenvio até essa confirmação.
6. **Ensaio offline→online de pesagem:** criar pesagem sem rede, reconectar, verificar sincronização única por `client_operation_id` e revisar conflito sem perda de versões. Depois agrupar e validar releases Windows/Android; não reinstalar o celular a cada pacote local.
7. **OCR local financeiro:** testar no aparelho uma nota real ou amostra não sensível, conferir campos extraídos, revisão humana e baixa/anexo. O código local está em 98%, mas o teste com documento real ainda falta.
8. **OCR em nuvem:** após renovação ou ampliação da cota do provedor, testar leitura com custo e tratamento de limite controlados. Esta etapa é externa e não bloqueia o lançamento manual nem o OCR local.

As etapas 1, 2, 6 e 7 exigem ensaio na operação/aparelho; as etapas 4, 5 e 8 dependem de ambiente ou serviço externo. A etapa 3 pode continuar localmente. Itens marcados 100% na tabela estão concluídos em seu escopo de implementação; a homologação geral ainda depende das integrações e ensaios acima.

## Regra de entrega

Uma etapa só muda para **Concluído** quando houver arquivos alterados, teste/validação executada, commit e tag. Mudanças visuais também exigem build Windows confirmado.

## Entrega em validação

- Testes concluídos: regressão transversal com 26 cenários; neste pacote, três contratos Flutter e três testes de servidor aprovados.
- APK Android: `build/app/outputs/flutter-apk/app-debug.apk` — pacote atualizado em 15/09 com a Agenda por data prevista e OCR local reforçado; SHA-256 `62D6AC6C1F1977C5B6D16DD9A408FD825C55C206C85532CA7750A026C740FEFA`.
- Gate finalizado: checkpoint Git, contratos integrados e APK Android recompilado com Gradle 8.14, AGP 8.11.1 e Kotlin 2.2.20.
