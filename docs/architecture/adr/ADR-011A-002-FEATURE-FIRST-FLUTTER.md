# ADR 11A-002 — Flutter Feature-first

**Status:** Aceito.

Código exclusivo de domínio vive na feature proprietária. `core` recebe infraestrutura transversal; novas telas de negócio em `core` são proibidas.
