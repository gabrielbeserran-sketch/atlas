# Object storage Atlas

O ambiente usa uma API compatível com S3. Em desenvolvimento local, o serviço é o MinIO.
Nunca armazene credenciais no aplicativo Flutter, no Git ou em arquivos `.env` versionados.
